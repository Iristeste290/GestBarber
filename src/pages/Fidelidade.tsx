import { AppLayout } from "@/components/AppLayout";

const Fidelidade = () => {
  return (
    <AppLayout title="Programa de Fidelidade" description="Recompense seus clientes fiéis">
      <div className="space-y-4">
        <p>Sistema de fidelidade em desenvolvimento...</p>
      </div>
    </AppLayout>
  );
};

export default Fidelidade;
